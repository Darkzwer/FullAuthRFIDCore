import XCTest
@testable import FullAuthRFIDCore

final class FullAuthRFIDCoreTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FullAuthRFIDCore().text, "Hello, World!")
    }
}
